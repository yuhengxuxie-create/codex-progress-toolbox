# 使用 Codex 完成首次安装

本文件面向不想手工判断安装细节的用户。Codex 会读取根目录 `AGENTS.md`，
负责环境检查、执行脚本、验证和排障；用户只负责飞书界面操作、选择要监控的
对话，以及在本机终端输入凭据。

## 开始前

1. 把 Release ZIP 解压到长期目录，不要直接在压缩包预览窗口运行。
2. 在 Codex 桌面应用中打开解压后的仓库根目录。
3. 新建任务并发送：

   ```text
   请按照 AGENTS.md 的首次安装流程安装这个项目。从创建飞书自定义机器人
   开始逐步引导我。任何 Webhook、签名密钥和 API Key 都只能让我在本机
   无回显终端输入，不要让我发到聊天中。完成每一步后先验证再继续。
   ```

## 你会经历的流程

| 阶段 | Codex 负责 | 用户负责 |
| --- | --- | --- |
| 飞书准备 | 解释界面、安全选项和错误码 | 建群、添加自定义机器人、保管凭据 |
| 环境安装 | 检查 PowerShell/Python，运行安装脚本 | 同意必要的本机配置修改 |
| 安全配置 | 启动无回显配置向导 | 在终端输入 Webhook 与可选签名密钥 |
| 对话选择 | 打开管理器或列出对话 | 勾选希望监控的对话 |
| 验证 | 运行配置校验、dry-run 和测试 | 确认是否发送真实飞书测试消息 |
| 生效 | 检查配置加载边界 | 完全退出并重启 Codex |

## 为什么不是完全无人值守

飞书机器人创建、群权限和密钥输入属于用户授权行为，安全安装不应替用户点击、
伪造凭据或把密钥暴露给聊天记录。其余可验证步骤均由 Codex 和项目脚本完成。

## 安装完成标准

- `scripts\validate.ps1` 成功；
- `scripts\send-test.ps1 -DryRun` 输出四行预览；
- 自动化测试通过；
- 用户授权的真实测试消息抵达目标飞书群；
- 重启 Codex 后，选中对话正常完成一轮会收到通知；
- Git 状态中不存在 `config.local.json`、`.state/`、备份或密钥。
