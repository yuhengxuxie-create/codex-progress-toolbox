# TreasureChest 插件开发

插件放在 `<安装目录>\plugins\<插件名>`，每个插件必须包含 `manifest.json`。当前插件 SDK 主版本为字符串 `"1"`，与生态产品版本 1.5.0 相互独立。

清单必须使用相对路径，入口最终解析结果必须留在插件目录内；绝对路径、`..` 越界、重解析点与未知 SDK 主版本均应被拒绝。示例见 `plugins\example-hello`。

插件不得读取飞书 App Secret、DPAPI 文件、Codex 对话库或其他插件的私有配置；外部命令、网络访问和持久化数据应在文档中明确说明。
