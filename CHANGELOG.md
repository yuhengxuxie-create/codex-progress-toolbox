## v1.6.3

修复短暂恢复造成业务超时告警反复发送的问题，发送前复核故障状态，并减少每轮监测的重复元数据读取。详见 [更新说明](docs/RELEASE_NOTES_v1.6.3.md)。

## v1.6.2

修复更新检查限流处理与官方手动入口、Skills 目录浏览与客户端发现、会话名称保留及模型信息显示。斜杠入口直接列出指令说明，官方拒绝提供安全诊断。详见 [更新说明](docs/RELEASE_NOTES_v1.6.2.md)。

## v1.6.1

修复 Windows 首次设置入口报错 9009 及括号路径问题，统一启动脚本 CRLF 并增加构建字节检查。旧用户使用新升级包保留配置和绑定。详见 [升级说明](docs/RELEASE_NOTES_v1.6.1.md)。

# 1.6.0 · 2026-09-12

相对公开 v1.5.0，本版本增加功能中心与点指令入口、模糊搜索、通用成果文件投递、通信守护和远程救援、额度重置预警及双端记录，改进通知、消息续聊、百宝箱文字窗口和整套升级。旧用户首次使用快捷升级入口，飞书权限 scope 不增加，需配置卡片回调及可选底部菜单。面向用户的完整说明见 [v1.6.0 更新说明](docs/RELEASE_NOTES_v1.6.0.md)。下文保留各阶段和历史记录。

# Changelog

## 1.6.0

### 文件交付与升级改进

- 统一准确路径和文件身份校验，支持结构化文件属性解析、Windows盘符前缀及仅未提交本地失败的安全恢复；保留历史证据与未知投递，不模糊选择文件、不重启补发。
- 提供完整安装及旧v1.x升级包，补充图文入口、权限配置和迁移说明。
- 修复首次安装或v1.2迁移在Python尚未建立时的自动回滚阻塞。只有事务确认尚未就绪、无已有守护状态/任务/进程及自定义配置时可恢复原目录；无法确认的已有安装继续拒绝覆盖。
- 旧完整生态在同次升级中运行时安装失败时，凭已停服、恢复任务禁用、无新进程及配置/状态与备份一致的证据自动恢复。跳过停服核验、手动旧事务或状态已变化时不适用此例外。

### 审批提醒与网页引用修复

- 审批观察复用15秒监测列表，仅在待审批或核对待审批解除时读取完整本地轮次，减少重复扫描；新加或移除监测最多增加15秒缓存刷新等待，另计轮询和读取耗时。不改变心跳阈值。
- 普通网页来源不再因网址含有 link、download 等字样而误报附件发送失败；明确交付的文件仍按原规则校验。
- 补充 Desktop 正在等待批准时的独立提醒，发送前再次核对当前状态并持久去重。没有可信远程审批入口时，提示回到原任务审批框操作；回复 A 不会代为批准。

### 可靠性修订

- 飞书连接健康增加线程存活与失败观测，区分存活重连和连接已停止，仅记录固定安全原因码；失败提示不再承诺正在自动恢复。本轮不改变SDK清理或自动重启策略。
- X各端点持久记录429冷却，按响应等待提示和现有08–23小时槽安排实际重试，备用及其他来源继续；桌面兼容可选状态字段，区分本次核验、缓存与失败，不将全局检查时间冒充端点重试时间。
- 审核额度预警A/B规则：排除推测疑问、玩笑和过去动作，补官方故障补偿意向及低预测冲突说明；夜间不领取/提交预警，保留既有已发放公告、去重及未知状态。不能承诺解除X限流或零漏报。
- 保留同轮各段正式答复及后续更正，修复内部收尾覆盖早先答案和附带插件列表错当用户请求；CLI/API按段保留输入空间并统一通知规则。历史静默及结果未知状态不重置、不自动补发。
- 后台身份及权限维护命令显式隐藏控制台窗口；减少目录选择时未读取记录的重复路径处理，保留实际读取归属校验，不承诺消除所有心跳超时。
- 已成功投递的回顾文字不再仅因提到附件或成果而被误报为本轮缺少路径；明确新交付的缺路径提示与显式链接识别保持。
- 守护仅在确认业务真正恢复后清除当前超时错误，保留故障历史和未决投递状态；慢轮询记录有限阶段耗时，不伪造心跳或承诺消除所有延迟。
- 更新器失败时保留材料、日志及进度，不自动启动未经核实的程序，也不宣称已恢复或已尝试回滚；成功后才清理并重开。

### 机器人文字入口点前缀

- 直接自然语言入口及别名统一使用行首 ASCII 半角 `.`，例如 `.新建个人会话`、`.项目新会话`、`.查看指令列表`；旧无点精确入口仅提示新写法，未知点命令不会当作任务正文。
- 按钮、引用编号及新建任务正文保持；`/project`、`/commands` 和原生 `/`、`$` 写法兼容。机器人入口不消费暂存图片，只有精确 `.发送`、`.取消` 处理暂存。现行文字指南同时更新新建回复、通知与独立附件说明，数据库仍为 schema23。

### 成果文件与新建对话

- 修复真实 SDK 的媒体上传参数调用：`kind` 按 keyword-only 契约传入；增加适配器经过真实 SDK 到 HTTP 边界的文件/图片回归，以及安全异常类型诊断。修正媒体上传契约不匹配导致的真实文件上传失败。

- 本轮明确交付的本机文件保存快照并逐件投递，原生生成图独立发送；文字摘要静默不抑制附件，首次启用不补发历史，结果未知不盲重发。
- 业务库升级至 schema23，保留文件状态、失败通知身份及回复关联；通信库持久保存上传结果，升级备份同时保留数据库与快照。安装预检在停服前拒绝不受支持的数据目录及 `.state` 内的重解析链接。
- 回复个人或项目新建提示可直接填写首轮正文；项目与运行方式先明确选择，旧模板入口继续可用。

### 通信守护与通知修订

- 通信守护独占飞书连接，业务进程隔离运行；普通停止保留远程救援，完整退出和维护意图持久保存，绑定私聊可用 `.启动飞书机器人` 恢复业务。
- Windows 同用户监督按心跳和精确实例身份有限恢复守护，保存退避、熔断及脱敏日志；迁移已归属的旧登录自启任务，升级/回滚恢复任务和意图，百宝箱不再重复拉起业务。
- 通知按当前请求区分可审阅结果、直接答案、重要变化与内部过程，保留语义判断和持久去重；已关联的用户历史消息可继续引用续聊，数据库升级至 schema22。
- 大型媒体使用私有文件引用控制进程间复制；新 Windows 登录仅在明确保存 running 且登录身份变化时一次性恢复，普通崩溃仍遵循明确启动和既有故障策略。

以下为开发阶段功能记录，已汇入 v1.6.0。

### 共享会话搜索引擎

- 新增飞书和 TreasureChest 共用的本机会话搜索引擎；C# 端只通过稳定 CLI/JSON 契约调用，不复制 Python 搜索规则。
- 支持会话名称、会话描述、最后活动时间等任意自然线索，允许留空、记忆偏差和字段错位；“这几天”固定按北京时间最近 5 天解释。
- 先按本地标题、项目、时间和历史零模型缩圈，再由 `gpt-5.6-luna` 低消耗分批评分；普通范围最多先深评 12 个候选，`all` 范围按 6 个一批真实续搜，直到完成或后续候选已不可能超过现有结果。
- 搜索描述、证据、最后结果和语义判断按查询与会话内容哈希持久缓存；会话内容变化后旧缓存失效。
- 新内容哈希写入不再物理删除旧缓存或判断；精确哈希保证不会误命中，旧版本仅由既有 90 天清理窗口回收。
- `session-search` 增加 `persistent|ephemeral` 缓存模式，默认持久模式服务真实用户；隔离模式在线备份到临时 SQLite，成功、取消或异常都清理且不回写，CLI schema v1 与退出码不变。
- 唯一命中直接返回；多候选每页最多 3 个；未找到时可在明确确认后按线索范围、最近 30 天、最近 180 天、全部用户会话逐级扩圈。
- 最后活动时间取最后一个存在最终文本或生成图片的已完成回合 `completedAt`，跳过失败、打断和空结果；最后结果复用进度总结规则。
- 新增 UTF-8 `session-search` CLI v1：请求文件或标准输入、原子进度 JSON、取消文件、纯 JSON stdout；成功/失败/取消分别使用退出码 0/2/3。

### 飞书 Codex 管理

- 新增顶层“搜索会话”引用表单，以及翻页、选择、取消和逐级扩圈上下文；所有列表继续与被引用的 `message_id` 精确绑定。
- 扩圈前显示预计候选数量、Luna 调用批次、额度和耗时；只有引用对应提示并完整回复“确认增加搜索范围”才执行。
- 对未知、损坏或不完整会话记录给出警告，不把不确定信息包装成确定结果。
- 搜索候选页将每个候选独立分段，保留匹配度与匹配说明；唯一命中和选择候选后的详情与“查询个人会话 → 选定 p01”共用同一消息块规则。
- 飞书富文本只加粗有限字段名，候选标题、正文、链接和命令保持普通字重，并稳定保留块间空行。
- 同一条有效普通进度通知或会话概览可接受多轮引用；每条入站消息是独立持久子投递，FIFO 且恰好一次。回执区分“已排队/已追加”，RPC、审批、人工输入和 Hook 仍为一次性。

### TreasureChest 百宝箱

- 项目监测管理页新增互斥的“精确搜索 / 模糊搜索”：精确模式匹配完整项目名，模糊模式打开三字段条件窗并显示后端真实进度。
- 搜索结果每页最多 3 个，支持扩大范围确认、候选详情展开、命中行高亮与居中；“全部会话”默认包含归档并显示长期/临时/未监测状态。
- 双击候选只有在 Codex 已运行时才跳转；未运行时明确提示但不强制启动。
- 搜索、监测和工具等 7 张表支持实时拖动列宽；相关窗口支持最大化/还原，并保留长期监测与临时监测的清晰文案。
- 修复主窗口在不同 DPI 环境间移动时的重复缩放：以唯一 96-DPI 逻辑基线记录布局，在创建、`WM_DPICHANGED`、最大化和还原时按目标 DPI 直接重算控件、表格、边距、最小尺寸与字体。
- 无边框窗口四边四角改用消息屏幕坐标、窗口矩形和当前 DPI 做命中；标题栏边缘优先进入 resize，最大化时禁用 resize，右下角 grip 使用真实坐标。
- 标题栏三个按钮改为等矩形自绘最小化线、最大化框、还原双框和关闭 X；最小化横线回到严格 `center.Y`，并以视觉包围盒验证三个图标在 96/120/144/192 DPI 下共用视觉中心，避免 Unicode 字形基线或人工偏移造成错位。
- 项目监测管理顶部七项控件改用真实 `MonitorToolbarPanel`，产品和自测共用 `ProjectMonitorToolbar/CenterControl` DPI 模型，整体垂直居中并设置防挤压最小宽度。
- 主窗与管理窗的全部抽屉改用共享 `DrawerRowPresentation`：展开组头深蓝白字，未展开组头使用既有浅蓝 RGB(204,218,250)，普通子会话缩进白底，选中子会话使用既有中性灰 RGB(236,236,236)；仅末子行底部绘制固定 1 设备像素柔和蓝灰分隔线，明确排除被否决的暖黄、左右线、包围框与粗底线。
- 抽屉绘制使用弱引用表防止重复订阅，并复用进程生命周期内的普通/粗体字体和分隔线画刷；刷新继续保留真实行 `Tag`、排序、索引、双击和滚动行为。
- 新增图标适配主题：集中瓷白、深海军蓝、薰衣草紫与灰紫令牌覆盖主窗、管理、设置、搜索、更新、编辑和托盘；新增 `ThemedProgressBar`，并保留 DPI、标题按钮和抽屉行为契约。
- 普通启动、开机启动和用户搜索显式使用 `persistent`；只有 `--qa-session-search-ephemeral` 首次启动链贯穿隔离模式，并显示“隔离 QA”。

### 已知流程事故

- 2026-08-29 两次 UI 验收误用默认持久模式，约 69 条证据缓存被刷新，旧逻辑删除 79 条旧内容哈希判断；运行前没有一致性数据库备份，无法完整安全恢复，没有部分恢复或 Luna 重建。消息、监测、会话与管理数据没有丢失。

### 兼容性与注意事项

- 深度语义搜索会消耗 Codex/Luna 额度并可能持续较长时间，全部范围还可能产生多页结果；界面和飞书提示会在执行前说明预计成本。
- 会话历史来自本机可读记录；被删除、损坏、未知或尚未写入最终结果的记录可能无法完整搜索，程序会保留警告而不是猜测。
- 本条目不改变已发布产品版本 1.5.0；发布、安装包与升级包仍需在用户明确授权后重新构建和验收。

## v1.5.0 - 2026-08-27

### 整体架构

- 从“单向完成通知脚本”升级为“Codex 管理 + 进度监测 + 百宝箱”三件套生态。
- 飞书通道由群自定义机器人 Webhook 升级为企业自建应用机器人，通过官方 WebSocket 长连接接收消息，通过官方 API 主动发送；无需公网回调服务器或电脑飞书客户端。
- Codex 管理与进度监测在同一安全后台内运行，百宝箱通过稳定 CLI/JSON 契约调用后台，不直接复制业务规则或改写 YAML。

### Codex 管理（飞书）

- 新增项目列表、个人会话列表、稳定分页快照和历史消息复用；项目/会话标号与被回复的飞书 message_id 精确绑定，避免串线。
- 支持从飞书新建项目、新建项目会话、新建个人会话、选定并继续已有 Codex 会话。
- 支持双向图片：手机图片与文字可在同一富文本中转交；纯图片可短时暂存后与下一条文字合并；Codex 生成图以飞书图片消息直接预览，并绑定原会话继续回复。
- 选定含图会话时会随概览发送最近一轮原图；上传前不转码、不压缩。
- 新增“等待用户选择”结构化通知，任务进入 `waitingOnUserInput` 时立即提醒，不需要等 `/goal` 暂停或结算，也不调用摘要模型。
- 新增用户全局 PermissionRequest 飞书审批：支持“允许一次”“拒绝”；只有 Codex 明确给出可复用 `prefix_rule` 时才提供“允许类似操作”，并先经 `codex execpolicy check` 验证后写入独立规则文件。
- 新增首轮命令“查询剩余额度”，通过官方 App Server `account/rateLimits/read` 读取，只显示 Codex 每周可用百分比与剩余重置卡数量，不展示 Spark，不使用缓存估算。
- 每条普通工作进度通知末尾自动附加相同的额度摘要；额度接口临时失败不阻断主进度通知。
- 新增飞书漫画课堂版使用说明（6 张 Q 版图片）和文字版使用说明 v1.5.0；发送“使用说明”默认发课堂图片，文字版按需发送。
- 入站命令、引用回复、表单、图片和审批全部按唯一 open_id 白名单、message_id、签名上下文与幂等键校验。

### Codex 进度监测

- 从仅处理正常完成事件，升级为结合 Codex notify、Codex Desktop 官方任务工具和只读结构化 SQLite 状态，可靠识别完成、失败、打断、待审批、待用户输入等状态。
- 新增自动发现与统一监测注册表：手动项永久保留，自动项 24 小时无活动后自然到期；用户明确移除会写抑制记录，避免自动立即恢复。
- 新增稳定 CLI：`monitor-list`、`monitor-add`、`monitor-remove`、`monitor-settings`，JSON stdout 保持纯净并带 `schema_version=1`。
- 新增自动监测热开关：关闭后停止自动发现/续期，不删除已有 auto 项，已有项按原到期时间自然退出；manual 项不受影响。
- 进度摘要改为优先保留清楚的原回复；短回复直接使用，长回复只做删减式凝练，避免把大白话改成机器化报告。
- 新增通知持久化去重、消息分片绑定、即时回执、图片失败隔离、有限重试、崩溃恢复和日志保留策略。
- 使用 Codex Desktop 官方 `list_threads`、`wait_threads`、`send_message_to_thread` 等能力，不接管桌面会话，不修改代理、路由、DNS 或 TUN。

### 百宝箱

- 会话管理页上方升级为六列“项目监测列表”：会话名称、项目/个人归属、来源、最后活动时间、剩余有效时间、完整任务 ID。
- 支持刷新、批量手动添加、批量明确移除、在 Codex 中打开任务，并按项目/个人来源分组展示。
- 新增“监测设置”界面，可真实开启/关闭自动监测；通过 `monitor-settings` CLI 热生效，不直接编辑 FeiShuBOT 配置。
- 由旧 YAML 直写服务和旧双栏管理器迁移到集中 `ProjectMonitorCliService` 适配器；兼容 numeric Unix 秒/毫秒和 ISO-8601 时间。
- 修复带空格 Python 路径经 `cmd /c` 启动失败的问题，改用安全 `call "python.exe" ...` 调用。
- 保留后台服务管理、托盘单实例、插件中心、工具启动、开机自启与有限自动重启功能。
- 当前百宝箱生产只读联调已验证六列字段完整、UI 无接口错误；v1.5.0 发布构建要求 0 warning/0 error 且全部自测通过。

### 安全与可靠性

- 飞书 App Secret 使用 DPAPI 保存，不写入 YAML、命令行、日志或分享包。
- 分享包排除真实 `config.yaml/config.json`、`.secrets`、`.state`、logs、数据库、DPAPI 文件、open_id、App ID、用户名、本机绝对路径、缓存、运行时状态和私人截图。
- Python/飞书依赖使用固定版本与 SHA-256；离线 Python 安装器必须验证 Python Software Foundation 数字签名。
- 所有安装、升级、卸载脚本必须备份现有 Codex notify/hooks，并能精确回滚本工具自己的改动，不覆盖其他工具配置。

### 破坏性变更与迁移说明

- GitHub v1.2.0 的“飞书自定义机器人 Webhook”不能直接作为 v1.5.0 企业自建应用机器人的 App ID/App Secret 使用。升级向导明确要求用户新建或选择企业自建应用、开通最小权限并重新绑定 open_id。
- v1.2.0 的 `config.local.json`/环境变量不能直接覆盖新版 `config.yaml`。升级器只迁移可验证的 thread-id 监测列表，其他旧配置完整备份并保留。
- 从 2026-08-25 生态包升级时，保留用户的 `config.yaml`、`.secrets`、`.state`、监测数据库和百宝箱 `config.json`，让状态库按程序 schema 正常迁移；模板配置不覆盖生产配置。
- 升级先停止旧服务、建立带时间戳备份、验证新包、执行迁移、启动并健康检查；失败时自动回滚文件和原 notify/hooks，不得留下半升级状态。

## v1.2.0 - 2026-08-26

旧公开基线：群自定义机器人 Webhook、`agent-turn-complete` 四行通知、手选 thread-id 和可选外部摘要。该版本不包含双向飞书管理、企业自建应用、全局审批、额度查询或 TreasureChest。保留旧 Release 仅用于审计和升级来源识别。
