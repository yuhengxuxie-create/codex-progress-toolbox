# v1.6.3

- 修复业务状态超时告警可能反复发送的问题：短暂恢复后再次超时仍视为同一次故障，连续稳定恢复后才结束故障记录；已经恢复的旧告警不会继续发送。
- 减少每轮监测中重复读取会话元数据的开销，改善会话较多时的轮询效率。

业务超时检测仍然保留。实际响应速度取决于会话数量、磁盘和客户端负载，不能保证所有负载下都不会超时。

已有用户下载升级包，解压到原安装目录之外，运行“快捷升级.cmd”并选择原安装目录。升级保留配置、绑定及用户数据，无需重新配对或新增飞书权限。新用户选择 full 完整安装包，按包内安装指引配置自己的机器人。
